<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ezen.burger.dao.EventDAO">
	
	<select id="getAllEvents"  statementType="CALLABLE" parameterType="java.util.HashMap">
	{CALL getAllEvents(
	#{ref_cursor , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=BurgerVO }
	)}
	</select>
	
	<select id="getOngoingEvents"  statementType="CALLABLE" parameterType="java.util.HashMap">
	{CALL getOngoingEvents(
	#{ref_cursor , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=BurgerVO }
	)}
	</select>
	
	<select id="getPastEvents"  statementType="CALLABLE" parameterType="java.util.HashMap">
	{CALL getPastEvents(
	#{ref_cursor , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=BurgerVO }
	)}
	</select>
	
	<select id="getDetailEvent"  statementType="CALLABLE" parameterType="java.util.HashMap">
	{CALL getDetailEvent(#{eseq},
	#{ref_cursor , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=BurgerVO }
	)}
	</select>
</mapper>